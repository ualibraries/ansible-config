---
# file: site_splunk_forwarder.yml

- name: Apply common configuration to baseline nodes
  hosts: ansible-test-hosts 
  remote_user: inolan
  become: yes
  become_user: root
  gather_facts: true
  roles:
    - site_splunk_forwarder.yml

